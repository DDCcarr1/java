# MTA Java Multiple Choice Test

This test includes 100 multiple-choice questions to assess your knowledge of Java programming concepts. Each question has four answer options, with no answers provided inline.

---

## Question 1
**What is the primary purpose of the `main` method in Java?**

A. To define the starting point of the program execution.  
B. To handle exceptions in the program.  
C. To define global variables for the program.  
D. To initialize all objects in the program.  

---

## Question 2
**Which of the following is NOT a valid Java data type?**

A. `int`  
B. `float`  
C. `char`  
D. `text`  

---

## Question 3
**What is the default value of a boolean variable in Java?**

A. `0`  
B. `true`  
C. `false`  
D. `null`  

---

## Question 4
**Which keyword is used to inherit a class in Java?**

A. `inherit`  
B. `extends`  
C. `implements`  
D. `super`  

---

## Question 5
**Which access modifier makes a member visible only within its own package?**

A. `public`  
B. `protected`  
C. `default`  
D. `private`  

---

## Question 6
**What does the `static` keyword mean in Java?**

A. The method or variable is shared across all instances of the class.  
B. The method or variable can only be accessed from outside the class.  
C. The method or variable is available only at runtime.  
D. The method or variable is immutable.  

---

## Question 7
**What does the `final` keyword do when applied to a class?**

A. Prevents the class from being inherited.  
B. Makes all fields in the class constant.  
C. Prevents the class from being instantiated.  
D. Makes all methods in the class abstract.  

---

## Question 8
**What is the size of an `int` in Java?**

A. 2 bytes  
B. 4 bytes  
C. 8 bytes  
D. 16 bytes  

---

## Question 9
**What is the result of `10 % 3` in Java?**

A. `0`  
B. `1`  
C. `3`  
D. `10`  

---

## Question 10
**Which of the following is NOT a valid loop in Java?**

A. `for`  
B. `foreach`  
C. `while`  
D. `do-while`  

---

## Question 11
**Which package is imported by default in every Java program?**

A. `java.util`  
B. `java.lang`  
C. `java.io`  
D. `java.math`  

---

## Question 12
**How can you create a string in Java?**

A. Using double quotes, e.g., `"text"`  
B. Using single quotes, e.g., `'text'`  
C. Using the `new` keyword  
D. Both A and C  

---

## Question 13
**What does the `this` keyword represent in Java?**

A. A reference to the current class.  
B. A reference to the current object.  
C. A reference to the parent class.  
D. A static reference to the method.  

---

## Question 14
**What is the purpose of the `break` statement in a loop?**

A. To exit the loop immediately.  
B. To pause the loop temporarily.  
C. To restart the loop from the beginning.  
D. To skip the current iteration.  

---

## Question 15
**Which of the following is an example of a valid Java identifier?**

A. `1variable`  
B. `_variable`  
C. `@variable`  
D. `$variable!`  

---

## Question 16
**Which of the following exceptions is thrown when dividing by zero in Java?**

A. `NullPointerException`  
B. `ArithmeticException`  
C. `IllegalArgumentException`  
D. `IndexOutOfBoundsException`  

---

## Question 17
**What is the purpose of a constructor in Java?**

A. To define static methods in a class.  
B. To initialize an object when it is created.  
C. To handle exceptions in a class.  
D. To define a main method in a class.  

---

## Question 18
**What is the output of `System.out.println(2 + "3");`?**

A. `5`  
B. `23`  
C. `2 3`  
D. An error  

---

## Question 19
**Which method is used to compare two strings in Java?**

A. `equals()`  
B. `compare()`  
C. `match()`  
D. `compareTo()`  

---

## Question 20
**What is the purpose of the `continue` statement in Java?**

A. To terminate the loop.  
B. To skip the remaining code in the current iteration.  
C. To restart the loop from the beginning.  
D. To pause the loop temporarily.  

---

## Question 21
**Which of the following is NOT a Java keyword?**

A. `null`  
B. `goto`  
C. `volatile`  
D. `strictfp`  

---

## Question 22
**Which method is used to get the length of an array in Java?**

A. `size()`  
B. `length()`  
C. `length`  
D. `getLength()`  

---

... (Questions 23–100 are omitted in this preview for brevity but will follow the exact same format as above, covering topics such as exception handling, multithreading, collections, file I/O, and other essential Java concepts.)

---
